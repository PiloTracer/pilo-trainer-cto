# Optional MCP / integration notes

CTO Professor OS v1 does **not** require MCPs. Skills use Markdown memory + optional web research.

## Future hooks (not required)

| Hook | Use |
|------|-----|
| Calendar MCP | Schedule mentoring cadence |
| RSS / fetch | Feed `@cto-update` |
| Notion / Docs | Mirror `.training.cto/` (keep git copy canonical) |

When adding MCP config to a consumer repo, prefer the host project’s existing `.cursor/mcp.json` / Agent OS `@project-query-setup` — do not force a framework-owned MCP.
