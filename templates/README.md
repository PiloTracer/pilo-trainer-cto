# Templates

| Path | Purpose |
|------|---------|
| `cursorrules.template` | Project `.cursorrules` (thin/fat) |
| `bootstrap.sh` | Create `.work.cto/` skeleton |
| `training/**/*.template` | Learner memory file templates |

Run: `bash templates/bootstrap.sh` from the framework root, or via `@cto-deploy-basic` / `@cto-bootstrap init`.
