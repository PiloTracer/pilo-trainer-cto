# Templates

| Path | Purpose |
|------|---------|
| `cursorrules.template` | Project `.cursorrules` (thin/fat) |
| `bootstrap.sh` | Create `.training.cto/` skeleton |
| `training/**/*.template` | Learner memory file templates |

Run: `bash templates/bootstrap.sh` from the framework root, or via `@deploy-basic` / `@cto-bootstrap init`.
